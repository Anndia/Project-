This code was created around pycharm, so it would be the most efficient to use pycharm to run the code if possible. But this should also work for other programs as well. As long as it can utilize import turtle, the program should run just fine.
